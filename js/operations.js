export const getSum = (a, b) => a + b;
export const getMinus = (a, b) => a - b;
export const getDev = (a, b) => a / b;
export const getMult = (a, b) => a * b;

// export {getSum, getMinus, getDev, getMult}

const operations = {
  getSum,
  getMinus,
  getDev,
  getMult,
};

export default operations;
